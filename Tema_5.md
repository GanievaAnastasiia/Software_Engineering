# Тема 5. Базовые коллекции: множества, списки 
Отчет по Теме #5 выполнил(а):
- Ганиева Анастасия Ильсуровна
- ПИЭ-22-1

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | + |
| Задание 4 | + | + |
| Задание 5 | + | + |
| Задание 6 | + | - |
| Задание 7 | + | - |
| Задание 8 | + | - |
| Задание 9 | + | - |
| Задание 10 | + | - |

знак "+" - задание выполнено; знак "-" - задание не выполнено;

Работу проверили:
- к.э.н., доцент Панов М.А.

## Лабораторная работа №1
### Друзья предложили вам поиграть в игру “найди отличия и убери повторения (версия для программистов)”. Суть игры состоит в том, что на вход программы поступает два множества, а ваша задача вывести все элементы первого, которых нет во втором. А вы как раз недавно прошли множества и знаете их возможности, поэтому это не составит для вас труда.

```python
def find_difference(set_1, set_2):
    return set_1 - set_2
set_1 = {'white', 'red', 'green', 'blue', 'pink', 'black'}
set_2 = {'white', 'red', 'green', 'blue'}
difference = find_difference(set_1, set_2)
print("Элементы из первого множества, которых нет во втором:", difference)


```
### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_5/images5/1.png)

## Выводы

Функция `find_difference`, которая принимает два параметра `set_1` и `set_2`. Она возвращает элементы, которые есть в `set_1`, но отсутствуют в `set_2`.
Множество: `set_1`, содержит шесть цветов, и `set_2`, содержит четыре цвета.
Функция `find_difference` вызывается с `set_1` и `set_2` в качестве аргументов, и результат сохраняется в переменной `difference`.
`print("Элементы из первого множества, которых нет во втором:", difference)` вывод результата



## Лабораторная работа №2
### Напишите две одинаковые программы, только одна будет использовать set(), а вторая frozenset() и попробуйте к исходному множеству добавить несколько элементов, например, через цикл.

```python
a = set('abcdefg')
print(a)
for i in range(1, 7):
    a.add(i)
print(a)
```
```python
a = frozenset('abcdefg')
print(a)
for i in range(1, 7):
    a.add(i)
print(a)
```
### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_5/images5/2.png)

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_5/images5/3.png)

## Выводы

В первом коде с set() всё работает корректно, получается изменяемое множество.
`set` изменяемое множество в Python. Используется для хранения уникальных элементов без определённых порядков.
Во втором коде с frozenset() происходит ошибка при попытке изменять его, так как frozenset нельзя изменять после его создания.
`frozenset` неизменяемое множество в Python. Оно также используется для хранения уникальных элементов, но не допускает изменений после создания



## Лабораторная работа №3
### На вход в программу поступает список (минимальной длиной 2 символа). Напишите программу, которая будет менять первый и последний элемент списка.

```python
def fun(lst):
    lst[0], lst[-1] = lst[-1], lst[0]
    return lst
my_list = [1, 2, 3, 4, 5]
print(fun(my_list))
```

### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_5/images5/4.png)

## Выводы

`lst[0]`  первый элемент списка 
`lst[-1]`  последний элемент списка
` my_list = [1, 2, 3, 4, 5]` создается список из пяти элементов
Функция f` un`  вызывается с ` my_list`  в качестве аргумента, и результат выводится на экран.
После выполнения кода, список `my_lis`t изменится на `[5, 2, 3, 4, 1]`, так как первый элемент (1) и последний элемент (5) поменяются местами.


## Лабораторная работа №4
### На вход в программу поступает список (минимальной длиной 10 символов). Напишите программу, которая выводит элементы с индексами от 2 до 6. В программе необходимо использовать “срез”.
```python
a=[87, 974, 33, 98, 11, 53, 89, 12, 37, 9711, 752, 541, 21, 9743, 642, 88]
print(a[2:6])
```
### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_5/images5/5.png)


## Выводы

`a[2:6]` в этой части кода извлекается "срез" списка a, начиная с элемента с индексом 2 и заканчивая элементом с индексом 5 (индикатор 6 не включается). Индексы в Python начинаются с нуля, 


## Лабораторная работа №5
### Иван задумался о поиске «бесполезного» числа, полученного из списка. Суть поиска в следующем: он берет произвольный список чисел, находит самое большое из них, а затем делит его на длину списка. Студент пока не придумал, где может пригодиться подобное значение, но ищет у вас помощи в реализации такой функции useless().

```python
def useless(lst):
    return max(lst) / len(lst)
print(useless([3, 6, 7, 4, 2]))
print(useless([12,55, 88.2, 77.9, -4, 22.5, 7.2, 34, -7]))
print(useless([19.8 ,-55, -2.2, 127.4, 4, 20.5, 2, 3, -74, 67]))
```
### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_5/images5/6.png)


## Выводы

Определение функции `useless:` Функция принимает один аргумент lst, который должен быть списком чисел. Она возвращает значение, полученное делением максимального элемента списка на количество элементов в списке.
`max(lst):` находит максимальный элемент в списке.
`len(lst):` определяет количество элементов в списке.


## Лабораторная работа №6
### Ребята не могут определится каким супергероем они хотят стать. У них есть случайно составленный список супергероев, и вы должны определить кто из ребят будет каким супергероем. Необходимо использовать разделение списков.

```python
superheroes = ['batman', 'spiderman', 'superman']
nikolay, ivan, vasiliy = superheroes

print('Николай - ', nikolay)
print('Иван - ', ivan)
print('Василий - ', vasiliy )

```
### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_5/images5/7.png)


## Выводы

Cоздаётся список `superheroes`, который содержит три строки: `'batman', 'spiderman' и 'superman'`.  В этой строке выполняется распаковка списка. Здесь переменным nikolay, ivan и vasiliy присваиваются значения из списка superheroes соответственно: `nikolay` получит `'batman'`; `ivan` получит `'spiderman'`; `vasiliy` получит `'superman'`. Вывод переменных.


## Лабораторная работа №7
### Вовочка, насмотревшись передачи “Слабое звено” решил написать программу, которая также будет находить самое слабое звено (минимальный элемент) и удалять его, только делать он это хочет не с людьми, а со списком. Помогите Вовочке с реализацией программы. Подсказка: для этого вам необходимо отсортировать список и удалить значение при помощи pop().

```python
a=[-7, 90, 67.4, 88, -67, 52.1, 3, 77.8, 3, 8, 83, -8, 2.8, 32]
a.sort()
print('Отсортированный список: \n' , a)
a.pop(0)
print('Отсортированный список без наименьшего значения: \n' , a)

```
### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_5/images5/8.png)


## Выводы

Создаётся список a, который содержит набор чисел (как целых, так и дробных).  Вызывая `a.sort()`, список будет отсортирован по возрастанию. Теперь элементы в списке будут располагаться от наименьшего к наибольшему. `print('Отсортированный список: \n', a) ` Эта строка выводит отсортированный список на экран. Метод `pop(0)` удаляет первый элемент списка. Это значение теперь не будет в списке. `print('Отсортированный список без наименьшего значения: \n', a)` Эта строка выводит изменённый список, в котором отсутствует наименьшее значение.

## Лабораторная работа №8

### Михаил решил создать большой n-мерный список, для этого он случайным образом создал несколько списков, состоящих минимум из 3, а максимум из 10 элементов и поместил их в один большой список. Он также как и Иван не знает зачем ему это сейчас нужно, но надеется на то, что это пригодится ему в будущем.

```python
import random
n = [random.sample(range(100), random.randint(3, 10)) for _ in range(5)]
print(n)
```

### Результат.
![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_5/images5/9.png)


## Выводы

`import random` модуль позволяет использовать функции для генерации случайных чисел и работы с ними.  `n = [random.sample(range(100), random.randint(3, 10)) for _ in range(5)]`.   Здесь используется списковое выражение для создания списка n, который состоит из 5 элементов.  `random.randint(3, 10):` Для каждой итерации генерируется случайное число от 3 до 10. Это число определяет, сколько уникальных случайных чисел нужно выбрать из следующего диапазона. `range(100)` создаёт последовательность чисел от 0 до 99. Каждый раз при выполнении кода вывод будет разным, так как используются генерация случайных чисел.


## Лабораторная работа №9

Вы работаете в ресторане и отвечает за статистику покупок, ваша задача сравнить между собой заказы покупателей, которые указаны в разном порядке. Реализуйте функцию superset(), которая принимает 2 множества. Результат работы функции: вывод в консоль одного из сообщений в зависимости от ситуации:
1	- «Супермножество не обнаружено»
2	– «Объект {X} является чистым супермножеством»
3	– «Множества равны»

```python
def superset(set1, set2):
    if set1 > set2:
        print(f'Объект {set1} является чистым супермножеством')
    elif set1 == set2:
        print('Множества равны')
    elif set2 > set1:
        print(f'Объект {set2} является чистым супермножеством')
    else:
        print("Супермножество не обнаружено")

if __name__ == '__main__':
    superset({1, 8, 4, 6}, {4, 6})
    superset({1, 8, 4, 6, 3, 7}, {4, 6, 3})
    superset({100, 80}, {9, 8})        
```

### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_5/images5/10.png)


## Выводы

 Функция `superset:`
   - Принимает два множества, set1 и set2.
   - Проверяет, является ли set1 чистым супермножеством set2 (set1 > set2).
   - Проверяет, равны ли множества (set1 == set2).
   - Проверяет, является ли set2 чистым супермножеством set1 (set2 > set1).
   - Если ни одно из условий не выполняется, выводит сообщение об отсутствии супермножества.

`if __name__ == '__main__':` вызываются три проверки с разными множествами.

## Лабораторная работа №10

Предположим, что вам нужно разобрать стопку бумаг, но нужно начать работу с нижней, “переверните стопку”. Вам дан произвольный список. Представьте его в обратном порядке. Программа должна занимать не более двух строк в редакторе кода.

```python
my_list=[2, 5, 7, 4]
print(my_list[::-1])
```

### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_5/images5/11.png)

## Выводы

Создаётся список `my_list`, состоящий из четырех элементов: 2, 5, 7 и 4.
`my_list[::-1]` развертывание списка.  Использование среза с `[::-1]` означает, что мы берём все элементы списка, но проходим через них в обратном порядке. Первое двоеточие : указывает, что мы берём все элементы списка. Второе двоеточие : указывает на шаг среза. Значение -1 означает, что идём в обратном направлении. Выводим результат. 



## Самостоятельная работа №1
### Ресторан на предприятии ведет учет посещений за неделю при помощи кода работника. У них есть список со всеми посещениями за неделю. Ваша задача посчитать:<br>• Сколько было выдано чеков<br>• Сколько разных людей посетило ресторан<br>• Какой работник посетил ресторан больше всех раз.<br>Список выданных чеков за неделю: [8734, 2345, 8201, 6621, 9999, 1234, 5678, 8201, 8888, 4321, 3365, 1478, 9865, 5555, 7777, 9998, 1111, 2222,333, 4444, 5556, 6666, 5410, 7778, 8889, 4445, 1439, 9604, 8201, 3365, 7502, 3016, 4928, 5837, 8201, 2643, 5017, 9682, 8530, 3250, 7193, 9051, 4506, 1987, 3365, 5410, 7168, 7777, 9865, 5678, 8201, 4445, 3016, 4506, 4506]<br>Результатом выполнения задачи будет: листинг кода, и вывод в консоль, в котором будет указана вся необходимая информация.

```python
checks = [
    8734, 2345, 8201, 6621, 9999, 1234, 5678, 8201, 8888,
    4321, 3365, 1478, 9865, 5555, 7777, 9998, 1111, 2222,
    333, 4444, 5556, 6666, 5410, 7778, 8889, 4445, 1439,
    9604, 8201, 3365, 7502, 3016, 4928, 5837, 8201, 2643,
    5017, 9682, 8530, 3250, 7193, 9051, 4506, 1987, 3365,
    5410, 7168, 7777, 9865, 5678, 8201, 4445, 3016, 4506,
    4506
]

num_checks = len(checks)
unique_people = set(checks)
num_unique_people = len(unique_people)
from collections import Counter
counter = Counter(checks)
most_common_person = counter.most_common(1)[0]  # работник с макс кол-во посещений

print(f'Количество выданных чеков: {num_checks}')
print(f'Количество разных людей, посетивших ресторан: {num_unique_people}')
print(f'Работник с ID {most_common_person[0]} посетил ресторан больше всех раз ({most_common_person[1]} раз).')

```

### Результат

![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_5/images5/12.png)

### Выводы

1 - Список чеков:  есть список `checks` с номерами выданных чеков.

2 - Счётчик чеков:  используем `len(checks)` для подсчета общего количества выданных чеков.

3 - Уникальные работники: С помощью `set(checks)` получаем уникальные номера чеков и затем считаем их количество.

4 - Наиболее частое посещение: Используем Counter из модуля` collections`, который создаёт словарь частотных значений, чтобы легко найти работника, который посещал ресторан больше всего раз. Метод `most_common(1)` возвращает самого популярного работника и количество его посещений.

## Самостоятельная работа №2
### На физкультуре студенты сдавали бег, у преподавателя физкультуры есть список всех результатов, ему нужно узнать<br>• Три лучшие результата<br>• Три худшие результата<br>• Все результаты начиная с 10<br>Ваша задача помочь ему в этом. Список результатов бега: [10.2, 14.8, 19.3, 22.7, 12.5, 33.1, 38.9, 21.6, 26.4, 17.1, 30.2, 35.7, 16.9, 27.8, 24.5, 16.3, 18.7, 31.9, 12.9, 37.4]. Результатом выполнения задачи будет: листинг кода, и вывод в консоль, в котором будет указана вся необходимая информация.


```python
results = [10.2, 14.8, 19.3, 22.7, 12.5, 33.1, 38.9, 21.6, 26.4, 17.1, 30.2, 35.7, 16.9,
           27.8, 24.5, 16.3, 18.7, 31.9, 12.9, 37.4]

results.sort()
best_three = results[-3:]
worst_three = results[:3]
from_ten = results

print(
    f'Топ-3 лучших результата: {best_three}\n'
    f'Топ-3 худших результата: {worst_three}\n'
    f'Все результаты начиная с 10-ти: {from_ten}'
    )
```

### Результат

![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_5/images5/13.png)

### Выводы

1 - Сортировка списка:
`results.sort()` сортирует список results по возрастанию. После этого самый низкий результат оказывается в начале списка, а самый высокий в конце.

2 - Топ-3 лучших результата:
`best_three = results[-3:]` извлекает последние три элемента из отсортированного списка, которые соответствуют лучшим результатам.

3 - Топ-3 худших результата:
`worst_three = results[:3]` извлекает первые три элемента из отсортированного списка, которые соответствуют худшим результатам.

4 -  Вывод всех результатов:
`from_ten = results` просто копирует отсортированный список результатов в новую переменную` from_ten`.

## Самостоятельная работа №3
### Преподаватель по математике придумал странную задачку. У вас есть три списка с элементами, каждый элемент которых – длина стороны треугольника, ваша задача найти площади двух треугольников, составленные из максимальных и минимальных элементов полученных списков. Результатом выполнения задачи будет: листинг кода, и вывод в консоль, в котором будут указаны два этих значения. Три списка:<br>one = [12, 25, 3, 48, 71]<br>two = [5, 18, 40, 62, 98]<br>three = [4, 21, 37, 56, 84]

```python
from math import sqrt

list = [[12, 25, 3, 48, 71], [5, 18, 40, 62, 98], [4, 21, 37, 56, 84]]
max_triangle = []
min_triangle = []


def count_area(a, b, c):
    p = (a + b + c) / 2
    s = sqrt(p * (p - a) * (p - b) * (p - c))
    return s


for partOfList in list:
    max_triangle.append(max(partOfList))
    min_triangle.append(min(partOfList))

print(f'S наибольшего равна {count_area(*max_triangle)}\nS наименьшего равна {count_area(*min_triangle)}')
```

  ### Результат

![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_5/images5/14.png)

### Выводы

1 - Импорт модуля:
`from math import sqrt` импортирует функцию, вычисляющую квадратный корень.

2 - Список с данными:
`list = [[12, 25, 3, 48, 71], [5, 18, 40, 62, 98], [4, 21, 37, 56, 84]]` списки, содержащие числа, которые будут использоваться для вычисления сторон треугольников.

3 - Создание пустых списков:
`max_triangle и min_triangle` — создаются для хранения максимальных и минимальных значений из каждого вложенного списка.

4 - `Функция count_area:`
Принимает три стороны треугольника (a, b, c) и вычисляет его площадь по формуле Герона:
     - p — полупериметр треугольника.
     - s — площадь треугольника, вычисляемая на основе полупериметра и сторон.

5 - Цикл для извлечения максимальных и минимальных значений:
Перебирает каждый вложенный список и добавляет максимальные и минимальные значения в соответствующие списки.

6-  Вывод результата:
Площадь треугольника с максимальными сторонами `(max_triangle)` и площадь треугольника с минимальными сторонами `(min_triangle)` вычисляются и выводятся


## Самостоятельная работа №4
### Никто не любит получать плохие оценки, поэтому Борис решил это исправить. Допустим, что все оценки студента за семестр хранятся в одном списке. Ваша задача удалить из этого списка все двойки, а все тройки заменить на четверки. Списки оценок (проверить работу программы на всех трех вариантах):<br>[2, 3, 4, 5, 3, 4, 5, 2, 2, 5, 3, 4, 3, 5, 4]<br>[4, 2, 3, 5, 3, 5, 4, 2, 2, 5, 4, 3, 5, 3, 4]<br>[5, 4, 3, 3, 4, 3, 3, 5, 5, 3, 3, 3, 3, 4, 4]<br>Результатом выполнения задачи будет: листинг кода, и вывод в консоль, в котором будут три обновленных массива.
  
```python
def fix_grades(grades):
    return [4 if grade == 3 else grade for grade in grades if grade != 2]
grades_list_1 = [2, 3, 4, 5, 3, 4, 5, 2, 2, 5, 3, 4, 3, 5, 4]
grades_list_2 = [4, 2, 3, 5, 3, 5, 4, 2, 2, 5, 4, 3, 5, 3, 4]
grades_list_3 = [5, 4, 3, 3, 4, 3, 3, 5, 5, 3, 3, 3, 3, 4, 4]
updated_grades_1 = fix_grades(grades_list_1)
updated_grades_2 = fix_grades(grades_list_2)
updated_grades_3 = fix_grades(grades_list_3)
print( updated_grades_1)
print( updated_grades_2)
print( updated_grades_3)
```

### Результат

![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_5/images5/17.png)

### Выводы

1 - Функция принимает список оценок grades. Создает новый список, в котором: оценка 3 заменяется на 4, а также оценки 2 исключаются из нового списка.Все остальные оценки остаются без изменений.
`def fix_grades(grades):`
    `return [4 if grade == 3 else grade for grade in grades if grade != 2]`

2 - Вывод результатов


## Самостоятельная работа №5
### Вам предоставлены списки натуральных чисел, из них необходимо сформировать множества. При этом следует соблюдать это правило: если какое-либо число повторяется, то преобразовать его в строку по следующему образцу: например, если число 4 повторяется 3 раза, то в множестве будет следующая запись: само число 4, строка «44», строка «444». <be>Множества для теста:<br>list_1 = [1, 1, 3, 3, 1]<br>list_2 = [5, 5, 5, 5, 5, 5, 5]<br>list_3 = [2, 2, 1, 2, 2, 5, 6, 7, 1, 3, 2, 2]<br>Результаты вывода (порядок может отличаться, поскольку мы работаем с set()):<br>{'11', 1, 3, '33', '111'}<br>{5, '5555', '555555', '55555', '555', '55', '5555555'}<br>{'11', 1, 3, 2, 5, 6, '222222', '222', 7, '2222', '22222', '22'}
  
```python
lists = [
    [1, 1, 3, 3, 1], 
    [5, 5, 5, 5, 5, 5, 5], 
    [2, 2, 1, 2, 2, 5, 6, 7, 1, 3, 2, 2]
]

def convert_to_set(arr):
    count_dict = {}
    result_set = set()

    for num in arr:
        if num in count_dict:
            count_dict[num] += 1
        else:
            count_dict[num] = 1


    for num, count in count_dict.items():
        result_set.add(num)
        
        for i in range(2, count + 1):
            result_set.add(str(num) * i)

    return result_set

for l in lists:
    print(convert_to_set(l))

```

### Результат

![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_5/images5/16.png)

### Выводы

1 - Списки: Заданы три списка, в которых могут повторяться числа.

2 - Функция `convert_to_set`:
   - Создаем словарь `count_dict`, чтобы отслеживать количество каждого числа.
   - В первом цикле считаем количество появлений каждого числа.
   - Во втором цикле формируем новое множество, добавляя само число и соответствующие строковые представления по количеству.

3 - Вывод результатов:
   - Для каждого входного списка вызывается функция `convert_to_set`, и результат выводится на экран.


## Общие выводы по теме

Списки 
- Упорядоченные коллекции, которые могут содержать дублирующиеся элементы и изменяться после создания.
- Добавление (append(), insert()), удаление  pop()), изменение элементов, срезы и другие методы.

Кортежи 
- Неизменяемые коллекции, которые могут содержать разные типы данных и использоваться для хранения фиксированных наборов значений.
- Не изменяемы после создания, что делает их безопасными для использования в качестве ключей в словарях.

Множества 
- Неупорядоченные коллекции уникальных элементов, которые не могут содержать дубликатов. Подходят для удаления повторяющихся элементов.
- Поддерживают множество операций, например, объединение (union()), пересечение (intersection()), разность (difference()).

