# Тема 5. Базовые коллекции: множества, списки 
Отчет по Теме #5 выполнил(а):
- Ганиева Анастасия Ильсуровна
- ПИЭ-22-1

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | + |
| Задание 4 | + | + |
| Задание 5 | + | + |
| Задание 6 | + | - |
| Задание 7 | + | - |
| Задание 8 | + | - |
| Задание 9 | + | - |
| Задание 10 | + | - |

знак "+" - задание выполнено; знак "-" - задание не выполнено;

Работу проверили:
- к.э.н., доцент Панов М.А.

## Лабораторная работа №1
### Друзья предложили вам поиграть в игру “найди отличия и убери повторения (версия для программистов)”. Суть игры состоит в том, что на вход программы поступает два множества, а ваша задача вывести все элементы первого, которых нет во втором. А вы как раз недавно прошли множества и знаете их возможности, поэтому это не составит для вас труда.

```python
def find_difference(set_1, set_2):
    return set_1 - set_2
set_1 = {'white', 'red', 'green', 'blue', 'pink', 'black'}
set_2 = {'white', 'red', 'green', 'blue'}
difference = find_difference(set_1, set_2)
print("Элементы из первого множества, которых нет во втором:", difference)


```
### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_5/images5/1.png)

## Выводы

Функция `find_difference`, которая принимает два параметра `set_1` и `set_2`. Она возвращает элементы, которые есть в `set_1`, но отсутствуют в `set_2`.
Множество: `set_1`, содержит шесть цветов, и `set_2`, содержит четыре цвета.
Функция `find_difference` вызывается с `set_1` и `set_2` в качестве аргументов, и результат сохраняется в переменной `difference`.
`print("Элементы из первого множества, которых нет во втором:", difference)` вывод результата



## Лабораторная работа №2
### Напишите две одинаковые программы, только одна будет использовать set(), а вторая frozenset() и попробуйте к исходному множеству добавить несколько элементов, например, через цикл.

```python
a = set('abcdefg')
print(a)
for i in range(1, 7):
    a.add(i)
print(a)
```
```python
a = frozenset('abcdefg')
print(a)
for i in range(1, 7):
    a.add(i)
print(a)
```
### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_5/images5/2.png)

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_5/images5/3.png)

## Выводы

В первом коде с set() всё работает корректно, получается изменяемое множество.
`set` изменяемое множество в Python. Используется для хранения уникальных элементов без определённых порядков.
Во втором коде с frozenset() происходит ошибка при попытке изменять его, так как frozenset нельзя изменять после его создания.
`frozenset` неизменяемое множество в Python. Оно также используется для хранения уникальных элементов, но не допускает изменений после создания



## Лабораторная работа №3
### На вход в программу поступает список (минимальной длиной 2 символа). Напишите программу, которая будет менять первый и последний элемент списка.

```python
def fun(lst):
    lst[0], lst[-1] = lst[-1], lst[0]
    return lst
my_list = [1, 2, 3, 4, 5]
print(fun(my_list))
```

### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_5/images5/4.png)

## Выводы

`lst[0]`  первый элемент списка 
`lst[-1]`  последний элемент списка
` my_list = [1, 2, 3, 4, 5]` создается список из пяти элементов
Функция f` un`  вызывается с ` my_list`  в качестве аргумента, и результат выводится на экран.
После выполнения кода, список `my_lis`t изменится на `[5, 2, 3, 4, 1]`, так как первый элемент (1) и последний элемент (5) поменяются местами.


## Лабораторная работа №4
### На вход в программу поступает список (минимальной длиной 10 символов). Напишите программу, которая выводит элементы с индексами от 2 до 6. В программе необходимо использовать “срез”.
```python
a=[87, 974, 33, 98, 11, 53, 89, 12, 37, 9711, 752, 541, 21, 9743, 642, 88]
print(a[2:6])
```
### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_5/images5/5.png)


## Выводы

`a[2:6]` в этой части кода извлекается "срез" списка a, начиная с элемента с индексом 2 и заканчивая элементом с индексом 5 (индикатор 6 не включается). Индексы в Python начинаются с нуля, 


## Лабораторная работа №5
### Иван задумался о поиске «бесполезного» числа, полученного из списка. Суть поиска в следующем: он берет произвольный список чисел, находит самое большое из них, а затем делит его на длину списка. Студент пока не придумал, где может пригодиться подобное значение, но ищет у вас помощи в реализации такой функции useless().

```python
def useless(lst):
    return max(lst) / len(lst)
print(useless([3, 6, 7, 4, 2]))
print(useless([12,55, 88.2, 77.9, -4, 22.5, 7.2, 34, -7]))
print(useless([19.8 ,-55, -2.2, 127.4, 4, 20.5, 2, 3, -74, 67]))
```
### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_5/images5/6.png)


## Выводы

Определение функции `useless:` Функция принимает один аргумент lst, который должен быть списком чисел. Она возвращает значение, полученное делением максимального элемента списка на количество элементов в списке.
`max(lst):` находит максимальный элемент в списке.
`len(lst):` определяет количество элементов в списке.


## Лабораторная работа №6
### Ребята не могут определится каким супергероем они хотят стать. У них есть случайно составленный список супергероев, и вы должны определить кто из ребят будет каким супергероем. Необходимо использовать разделение списков.

```python
superheroes = ['batman', 'spiderman', 'superman']
nikolay, ivan, vasiliy = superheroes

print('Николай - ', nikolay)
print('Иван - ', ivan)
print('Василий - ', vasiliy )

```
### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_5/images5/7.png)


## Выводы

Cоздаётся список `superheroes`, который содержит три строки: `'batman', 'spiderman' и 'superman'`.  В этой строке выполняется распаковка списка. Здесь переменным nikolay, ivan и vasiliy присваиваются значения из списка superheroes соответственно: `nikolay` получит `'batman'`; `ivan` получит `'spiderman'`; `vasiliy` получит `'superman'`. Вывод переменных.


## Лабораторная работа №7
### Вовочка, насмотревшись передачи “Слабое звено” решил написать программу, которая также будет находить самое слабое звено (минимальный элемент) и удалять его, только делать он это хочет не с людьми, а со списком. Помогите Вовочке с реализацией программы. Подсказка: для этого вам необходимо отсортировать список и удалить значение при помощи pop().

```python
a=[-7, 90, 67.4, 88, -67, 52.1, 3, 77.8, 3, 8, 83, -8, 2.8, 32]
a.sort()
print('Отсортированный список: \n' , a)
a.pop(0)
print('Отсортированный список без наименьшего значения: \n' , a)

```
### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_5/images5/8.png)


## Выводы

Создаётся список a, который содержит набор чисел (как целых, так и дробных).  Вызывая `a.sort()`, список будет отсортирован по возрастанию. Теперь элементы в списке будут располагаться от наименьшего к наибольшему. `print('Отсортированный список: \n', a) ` Эта строка выводит отсортированный список на экран. Метод `pop(0)` удаляет первый элемент списка. Это значение теперь не будет в списке. `print('Отсортированный список без наименьшего значения: \n', a)` Эта строка выводит изменённый список, в котором отсутствует наименьшее значение.

## Лабораторная работа №8

### Михаил решил создать большой n-мерный список, для этого он случайным образом создал несколько списков, состоящих минимум из 3, а максимум из 10 элементов и поместил их в один большой список. Он также как и Иван не знает зачем ему это сейчас нужно, но надеется на то, что это пригодится ему в будущем.

```python
import random
n = [random.sample(range(100), random.randint(3, 10)) for _ in range(5)]
print(n)
```

### Результат.
![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_5/images5/9.png)


## Выводы

`import random` модуль позволяет использовать функции для генерации случайных чисел и работы с ними.  `n = [random.sample(range(100), random.randint(3, 10)) for _ in range(5)]`.   Здесь используется списковое выражение для создания списка n, который состоит из 5 элементов.  `random.randint(3, 10):` Для каждой итерации генерируется случайное число от 3 до 10. Это число определяет, сколько уникальных случайных чисел нужно выбрать из следующего диапазона. `range(100)` создаёт последовательность чисел от 0 до 99. Каждый раз при выполнении кода вывод будет разным, так как используются генерация случайных чисел.


## Лабораторная работа №9

Вы работаете в ресторане и отвечает за статистику покупок, ваша задача сравнить между собой заказы покупателей, которые указаны в разном порядке. Реализуйте функцию superset(), которая принимает 2 множества. Результат работы функции: вывод в консоль одного из сообщений в зависимости от ситуации:
1	- «Супермножество не обнаружено»
2	– «Объект {X} является чистым супермножеством»
3	– «Множества равны»

```python
def superset(set1, set2):
    if set1 > set2:
        print(f'Объект {set1} является чистым супермножеством')
    elif set1 == set2:
        print('Множества равны')
    elif set2 > set1:
        print(f'Объект {set2} является чистым супермножеством')
    else:
        print("Супермножество не обнаружено")

if __name__ == '__main__':
    superset({1, 8, 4, 6}, {4, 6})
    superset({1, 8, 4, 6, 3, 7}, {4, 6, 3})
    superset({100, 80}, {9, 8})        
```

### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_5/images5/10.png)


## Выводы

 Функция `superset:`
   - Принимает два множества, set1 и set2.
   - Проверяет, является ли set1 чистым супермножеством set2 (set1 > set2).
   - Проверяет, равны ли множества (set1 == set2).
   - Проверяет, является ли set2 чистым супермножеством set1 (set2 > set1).
   - Если ни одно из условий не выполняется, выводит сообщение об отсутствии супермножества.

`if __name__ == '__main__':` вызываются три проверки с разными множествами.

## Лабораторная работа №10

Предположим, что вам нужно разобрать стопку бумаг, но нужно начать работу с нижней, “переверните стопку”. Вам дан произвольный список. Представьте его в обратном порядке. Программа должна занимать не более двух строк в редакторе кода.

```python
my_list=[2, 5, 7, 4]
print(my_list[::-1])
```

### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_5/images5/11.png)

## Выводы

Создаётся список `my_list`, состоящий из четырех элементов: 2, 5, 7 и 4.
`my_list[::-1]` развертывание списка.  Использование среза с `[::-1]` означает, что мы берём все элементы списка, но проходим через них в обратном порядке. Первое двоеточие : указывает, что мы берём все элементы списка. Второе двоеточие : указывает на шаг среза. Значение -1 означает, что идём в обратном направлении. Выводим результат. 

