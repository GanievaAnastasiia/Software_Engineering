# Тема 11. Итераторы и генераторы
Отчет по Теме #11 выполнил(а):
- Ганиева Анастасия Ильсуровна
- ПИЭ-22-1

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | - |
| Задание 4 | + | - |
| Задание 5 | + | - |

знак "+" - задание выполнено; знак "-" - задание не выполнено;

Работу проверили:
- к.э.н., доцент Панов М.А.

## Лабораторная работа №1

Простой итератор, но у него нет гибкой настройки, например его нельзя развернуть. Он работает просто как next(), но нет prev()

```python
numbers = [0, 1, 2, 3, 4, 5]
for item in numbers:
    print(item)
```
### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_11/images11/1.png)

## Выводы

Создается список `numbers`, содержащий значения от 0 до 5, и затем используется цикл `for`, чтобы пройтись по каждому элементу в этом списке и вывести его на экран.

## Лабораторная работа №2

Класс итератор с гибкой настройкой и удобными применением

```python
class CountDown:
    def __init__(self, start):
        self.count = start + 1
    def __iter__(self):
        return self
    def __next__(self):
        self.count -= 1
        if self.count < 0:
            raise StopIteration
        return self.count

if __name__ == '__main__':
    counter = CountDown(5)
    for i in counter:
        print(i)
```
### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_11/images11/2.png)

## Выводы

1. Класс CountDown предназначен для создания обратного отсчета от заданного числа до нуля.
2.`__init__(self, start)` метод является конструктором класса. Он принимает одно значение `start`, которое указывает начальное число для отсчета. Внутри метода создается переменная экземпляра self.count, которая инициализируется значением `start + 1`. Это сделано потому что при вызове метода next() сразу произойдет уменьшение значения счетчика на единицу.
3. Метод `iter(self)` возвращает объект, чтобы он мог использоваться в цикле `for`.
4. Метод `next(self)` выполняет интерации в циклах. При каждом вызове он уменьшает значение `self.count` на единицу. Если значение становится меньше нуля, генерируется исключение StopIteration, что сигнализирует о завершении цикла. В противном случае возвращается текущее значение `count`.

## Лабораторная работа №3

Генератор списка

```python
a = [i ** 2 for i in range(1, 5)]
print('a - ', a)
for i in a:
    print(i)
print('iter(a) - ', iter(a))
for i in a:
    print(i)
```
### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_11/images11/3.png)

## Выводы

1. Создается список квадратов чисел от 1 по 4 
2. Первый цикл for перебирает элементы списка a и выводит их по одному
3. `print('iter(a) - ', iter(a))` вызывается функция `iter()` для списка a, что возвращает итератор. 
4. Второй цикл for перебирает элементы списка a и выводит их по одному, как и в первом цикле. 

## Лабораторная работа №4

Выражения генераторы

```python
b = (i ** 2 for i in range(1, 5))
print(b)
print('first')
for i in b:
    print(i)
print('second')
for i in b:
    print(i)
```
### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_11/images11/4.png)

## Выводы

1. Вывод `print(b)`: выводится информация о  объекте
2. Вывод `print('first')`: печатает строку: first     
3. Цикл `for i in b`:
   - В первом шаге `i` будет равен 1, поэтому печатается `1 ** 2` 
   - Во втором шаге `i` будет равен 2, и печатается `2 ** 2`
   - В третьем шаге `i` будет равен 3, и печатается `3 ** 2`
   - В четвертом шаге `i` будет равен 4, и печатается `4 ** 2`
4. Вывод `print('second')`: печатает строку: second     
5. Цикл `for i in b`: снова: генератор b =не имеет больше значений для генерации. При выполнении второго цикла for не будет вывода.

## Лабораторная работа №5

Такой же счетчик, как и в первом задании, только это генератор и использует yield

```python
def countdown(count):
    while count >= 0:
        yield count
        count -= 1
if __name__ == '__main__':
    counter = countdown(5)
    for i in counter:
        print(i)
```
### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_11/images11/5.png)

## Выводы

1. `countdown` объявляет генератор, в цикле `while` будет вызывать `yield`, пока count не станет меньше 0.
2. В цикл for передается генератор counter:
   - `count` равен 5, и выводится 5.
   - `count` уменьшается на 1 и становится 4 тогда выводится 4.
   - это продолжается пока count не достигнет 0, которое будет выведено.
3. Когда `count` станет равным -1 цикл завершится.

## Самостоятельная работа №1

### Вас никак не могут оставить числа Фибоначчи, очень уж они вас заинтересовали. Изучив новые возможности Python вы решили реализовать программу, которая считает числа Фибоначчи при помощи итераторов. Расчет начинается с чисел 1 и 1. Создайте функцию fib(n), генерирующую n чисел Фибоначчи с минимальными затратами ресурсов. Для реализации этой функции потребуется обратиться к инструкции yield (Она не сохраняет в оперативной памяти огромную последовательность, а дает возможность “доставать” промежуточные результаты по одному). Результатом решения задачи будет листинг кода и вывод в консоль с числом Фибоначчи от 200.

```python
def fib(n):
    a, b = 1, 1
    count = 0
    while count < n:
        yield a
        a, b = b, a + b
        count += 1

for num in fib(300):
    print(num)
```

### Результат

![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_11/images11/6.png)

### Выводы

1. Функция `fib(n)` инициализирует два значения последовательности Фибоначчи: a и b, которые оба равны 1. Переменная count инициализируется 0 и следит за количество сгенерированных чисел.
2. В цикле `while:`
   - Используя `yield`, которая возвращать текущее значение a.
   - После обновляются значения: a получает значение b,  b получает сумму a + b, что равно следующему числу в последовательности Фибоначчи.
   - count увеличивается на 1 для отслеживания количества сгенерированных чисел.
3. Цикл будет повторяться до тех пор, пока не будет достигнуто n (у нас это 300).

## Самостоятельная работа №2
### К коду предыдущей задачи добавьте запоминание каждого числа Фибоначчи в файл “fib.txt”, при этом каждое число должно находиться на отдельной строчке. Результатом выполнения задачи будет листинг кода и скриншот получившегося файла

```python
def fib(n):
    a, b = 1, 1
    count = 0
    with open('fib.txt', 'w') as txt:
        while count < n:
            yield a
            a, b = b, a + b
            txt.write(str(a) + '\n')
            count += 1

print(*[i for i in fib(300)])
```

### Результат

![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_11/images11/7.png)
![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_11/images11/8.png)

### Выводы
1. `def fib(n):` определяется функция, которая принимает одно целое число n,равное количеству чисел Фибоначчи
2. Открытие файла: `with open('fib.txt', 'w') as txt`: файл  в режиме записи `('w')`. 
3. Цикл генерации чисел Фибоначчи:
   - `while count < n`: цикл выполняется, пока count меньше n. На каждой итерации цикла бсгенерировано одно число 
   - `yield a` — текущая версия a  возвращается из функции.
4. Запись в файл: `txt.write(str(a) + '\n')` запись текущего значения a в файл.
5. Вывод чисел Фибоначчи в консоль: `print(*[i for i in fib(300)])` создается список из 300 чисел Фибоначчи. Звездочка `(*)` распаковывает элементы списка и выводит их через пробел в консоль.

- При запуске программы, будет создан файл `fib.txt`, который будет содержать 300 чисел Фибоначчи, а также все 300 чисел будут выведены в консоль.
  
## Общие выводы по теме

1. Итераторы (iterators) -  объекты в Python, которые позволяют последовательно обходить элементы в какой-либо структуре данных, например, в списке, кортеже, множестве или строке
2. Генераторы в Python - специальный тип функций или выражений, которые позволяют создавать итераторы более эффективным способом. Генераторы позволяют генерировать значения последовательно по одному элементу за раз без необходимости хранения всей последовательности в памяти.
3. Генераторные выражения (generator expressions) в Python - компактный и эффективный способ создания генераторов без явного определения функции с использованием ключевого слова yield. Генераторные выражения позволяют создавать итераторы для обработки последовательностей данных на лету, что экономит память и упрощать код
4. Плюсы использования генераторов:
- экономия памяти
- эффективная обработка больших данных
- ленивая итерация
- удобство и компаткность 