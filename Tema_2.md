# Тема 2. Базовые операции языка Python
Отчет по Теме #2 выполнил(а):
- Ганиева Анастасия Ильсуровна
- ПИЭ-22-1

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | + |
| Задание 4 | + | + |
| Задание 5 | + | + |
| Задание 6 | + | + |
| Задание 7 | + | + |
| Задание 8 | + | + |
| Задание 9 | + | + |
| Задание 10 | + | + |

знак "+" - задание выполнено; знак "-" - задание не выполнено;

Работу проверили:
- к.э.н., доцент Панов М.А.

## Лабораторная работа №1
### Выведите в консоль три строки. Первая – любое число. Вторая – любое число в виде строки. Третья – любое число с плавающей точкой.

```python
print(123)
print('123')
print(1.23)
```
### Результат.
![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_2/images2/1.png)

## Выводы

Работа с функцией `print()`. Рассмотрим три строки и значения, которые они вывели:

1. `print(123)`: выводится целое число 123. Это число не взаимодействует со строковыми операциями и выводится как есть.

2. `print('123')`: Выводится строка '123', так как она заключена в одинарные кавычки. В этом случае это текстовая строка, а не число.

3. `print(1.23)`: Выводится число с плавающей точкой 1.23. Так же, как и в первом случае, оно выводится как числовое значение.

## Лабораторная работа №2
### Выведите в консоль три строки. Первая - результат сложения или вычитания минимум двух переменных типа int. Вторая - результат сложения или вычитания минимум двух переменных типа float. Третья - результат сложения или вычитания минимум двух переменных типа int и float.

```python
print(15678-4321)
print(7.1+7.54)
print(9+3.13+6+9.21)
```

### Результат
![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_2/images2/2.png)

### Выводы

Выводятся три строки с использованием функции `print()`. Каждая строка содержит разные значения:

1. `print(15678-4321)`: Выводится целое число 11357 типа int. Это результат вычитания двух целочисленных пересенных типа int.

2. `print(7.1+7.54)`: Выводится число с плавающей точкой 14.64 типа float. Это результат сложения двух переменных типа float.

3. `print(9+3.13+6+9.21)`: Выводится число с плавающей точкой 27.34 типа float. Это результат сложения двух переменных типа int и двух переменных типа float.

## Лабораторная работа №3
### Выведите в консоль три строки. Первая - обычная строка. Вторая - F строка с использованием заранее объявленной переменной. Третья - сложите две или более строк в одну.

```python
name = "Anastasiia"
print("Просто строка")
print(f"Привет, {name}!")
greeting = "Как прошел твой день?"
full_message = f"Привет, {name}! {greeting}"
print(full_message)
```

### Результат

![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_2/images2/3.png)

### Выводы

Выводятся три строки с использованием функции print(), каждая из которых имеет свои уникальные значения:

1. `print("Это обычная строка.")`: Выводится простая строка "Это обычная строка.".

2. `print(f"Привет, {name}!")`: Выводится строка "Привет, Anastasiia!", в которой подставлено значение переменной name.

3. `print(full_message)`: Выводится одна строка "Привет, Anastasiia! Как дела?", которая объединяет две строки, хранящиеся в переменных greeting и name, в одну целостную строку без дополнительных разделителей.

## Лабораторная работа №4
### Выведите в консоль три строки. Первая - трансформация любого типа переменной в bool. Вторая - трансформация любого типа переменной в float или int. Третья - трансформация любого типа переменной в str.

```python
one = 0
print(bool(one))

two = 10
print(float(two))

there = [1, 2, 3]
print(str(there))
```

### Результат

![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_2/images2/4.png)

### Выводы

Выводятся три строки с использованием функции print(). Каждая строка демонстрирует разные преобразования переменных и содержит соответствующие значения:

1. `print(bool(one))`: Выводится значение False, так как переменная one, равная 0, преобразована в логическое значение с помощью функции bool(), и 0 соответствует False.

2. `print(float(two))`: Выводится значение 10.0, так как переменная two, равная 10, преобразована в дробное число с помощью функции float().

3. `print(str(there))`: Выводится строка [1, 2, 3], так как переменная there, представляющая список [1, 2, 3], была преобразована в строку с помощью функции str().


## Лабораторная работа №5
### Присвойте трем переменным различные значения, воспользовавшись функцией input()

```python
name = input('Имя: ')
season = input('Время года: ')
month = input('Месяц: ')
print(f'Добрый день {name}! Чудесное время года {season} , на дворе уже  - {month}.')
```

### Результат

![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_2/images2/5.png)

### Выводы

Используется функция input() для получения значений от пользователя и выводится одна строка с использованием функции print():

1. `name = input('Имя: ')`: Пользователю предлагается ввести свое имя. Введенное значение будет сохранено в переменной name.

2. `season = input('Время года: ')`: Пользователю предлагается ввести время года. Введенное значение будет сохранено в переменной season.

3. `month = input('Месяц: ')`: Пользователю предлагается ввести месяц. Введенное значение будет сохранено в переменной month.

4. `print(f'Добрый день {name}! Чудесное время года {season} , на дворе уже  - {month}.')`: Выводится строка, содержащая приветствие, основанное на введенных значениях. Например, при вводе имени "Анастасия", времени года "осень" и месяца "сентябрь" выводится строка "Добрый день Анастасия! Чудесное время года осень, на дворе уже - сентябрь."

## Лабораторная работа №6
### Создайте две любые числовые переменные и выполните над ними несколько математических операций: возведение в степень, обычное деление, целочисленное деление, нахождение остатка от деления. При желании вы можете проверить как работают эти вычисления с разными типами данных, например, сначала создать две переменные int, затем создать две переменные float и наконец создать переменные типа int и float и провести над ними операции, прописанные выше.

```python
x = 28
y = 4
print('Возведение в степень: ', x**y)
print('Обычное деление: ', x/y)
print('Целочисленное деление: ', x//y)
print('Нахождение остатка от деления: ', x%y)
```

### Результат

![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_2/images2/6.png)
### Выводы

Выполняются различные математические операции с двумя числами x и y, и результаты выводятся с помощью функции print(). Вот вывод для каждой строки:

1. `print('Возведение в степень: ', x**y)`: Выводится результат возведения x в степень y, который равен 28**4 = 614656. Соответственно, строка: "Возведение в степень:  614656".

2. `print('Обычное деление: ', x/y)`: Выводится результат деления x на y, который равен 28/4 = 7.0. Строка будет: "Обычное деление:  7.0".

3. `print('Целочисленное деление: ', x//y)`: Выводится результат целочисленного деления x на y, который равен 28//4 = 7. Строка будет: "Целочисленное деление:  7".

4. `print('Нахождение остатка от деления: ', x%y)`: Выводится остаток от деления x на y, который равен 28%4 = 0. Строка будет: "Нахождение остатка от деления:  0".

## Лабораторная работа №7
### Создайте любую строковую переменную и произведите над ней математическое действие умножение на любое число.

```python
warning = "Стой! Опасная зона!"
print(warning*2)
```

### Результат

![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_2/images2/7.png)

### Выводы

Строка, хранящая предупреждение, выводится дважды с помощью множителя. Вот вывод для данной строки:

1. `print(warning*2)`: Выводится значение переменной warning, повторенное дважды. Если умножить строку на число типа int, то выведется строка столько раз, на сколько её умножили.

## Лабораторная работа №8
### Посчитайте сколько раз символ 'o' встречается в строке 'Hello World'.

```python
line = 'Hello World!'
print(line.count('o'))
```

### Результат

![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_2/images2/8.png)

### Выводы

Ппроисходит подсчет количества нахождений буквы 'o' в строке line. 

1. `print(line.count('o'))`: Метод count считает, сколько раз буква 'o' находится в строке 'Hello World!'. В данной строке 'Hello World!' буква 'o' находится 2 раза. 

## Лабораторная работа №9
### Напишите предложение 'Hello World' в две строки. Написанная программа должна занимать одну строку в редакторе кода.

```python
print('Hello\nWorld')
```

### Результат

![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_2/images2/9.png)

### Выводы

Строка 'Hello World' выводится на экран в две строки с использованием символа новой строки \n. 

1. `print('Hello\nWorld')`: Символ \n создает переход на новую строку, поэтому 'Hello' будет выведено на первой строке, а 'World' - на второй.

## Лабораторная работа №10
## Из предложения 'Hello World' выведите в консоль только 2 символ, а затем выведите слово 'Hello'

```python
line = 'Hello World!'
print(line[1])
print(line[:5])
```
### Результат

![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_2/images2/10.png)

### Выводы

В данном коде выполняются два вывода на экран: один символ и срез строки. 
1. `print(line[1])`: Этот вызов выводит символ строки line с индексом 1. В строке 'Hello World!' символ с индексом 1 - это 'e'.

2. `print(line[:5])`: Этот вызов выводит срез строки line, который включает все символы от начала до индекса 5 (не включая его). В данном случае это 'Hello'.

## Самостоятельная работа №1
### Выведите в консоль булевую переменную False, не используя слово False в строке или изначально присвоенную булевую переменную. Программа должна занимать не более двух строк редактора кода.

```python
a = 0
print(bool(a))
```

### Результат

![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_2/images2/11.png)

### Выводы

Переменной a присваивается значение 0, а затем выводится результат функции bool(a). 
Функция bool() возвращает False для значений 0, пустых коллекций и других эквивалентов «ложного». 

## Самостоятельная работа №2
### Присвоить значения трем переменным и вывести их в консоль, используя только две строки редактора кода

```python
a = 7;b = 4; c = 1
print(a, b, c, sep=', ')
```

### Результат

![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_2/images2/12.png)

### Выводы

В данном коде переменным a, b и c присваиваются значения 7, 4 и 1 соответственно. Функция print() выводит их, разделяя запятыми благодаря параметру sep=', '

## Самостоятельная работа №3
### Реализуйте ввод данных в программу, через консоль, в виде только целых чисел (тип данных int). То есть при вводе буквенных символов в консоль, программа не должна работать. Программа должна занимать не более двух строк редактора кода.

```python
num = int(input('введите целое число: '))
print(num)
```

  ### Результат

![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_2/images2/13.png)
![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_2/images2/13.1.png)

### Выводы

Переменной num присваивается целое число, введенное пользователем через функцию input(), которая выводит сообщение "введите целое число: ". Затем это число выводится на экран с помощью функции print().

## Самостоятельная работа №4
### Создайте только одну строковую переменную. Длина строки должна не превышать 5 символов. На выходе мы должны получить строку длиной не менее 16 символов. Программа должна занимать не более двух строк редактора кода.
  
```python
line = 'two'
print(line*8)
```

### Результат

![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_2/images2/14.png)

### Выводы

Переменной line присваивается строка 'two'. Выражение line * 8 означает, что строка 'two' будет повторена 8 раз подряд. В строке, которую мы получили в результате выполнения кода (twotwotwotwotwotwotwo), каждый повтор строки 'two' состоит из 3 символов. Поскольку мы повторили эту строку 8 раз, общее количество символов равно 24.

## Самостоятельная работа №5
### Создайте три переменные: день (тип данных - числовой), месяц (тип данных - строка), год (тип данных - числовой) и выведите в консоль текущую дату в формате: "Сегодня день месяц год. Всего хорошего!" используя F строку и оператор end внутри print(), в котором вы должны написать фразу "Всего хорошего!". Программа должна занимать не более двух строк редактора кода.
  
```python
import datetime; date=str(datetime.date.today().day); month=str(datetime.date.today().month); year= str(datetime.date.today().year)
print(f'Сегодня {date} {month} {year}', end='.\nВсего хорошего!')
```

### Результат

![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_2/images2/15.png)

### Выводы

 Импортируется модуль datetime, который позволяет работать с датами и временем.
2. Вычисляется текущий день, месяц и год, которые представляются в виде строк.
3. Форматируется строка с использованием f-строки, в которой выводится текст с текущей датой.
4. Выводится сообщение на экран с окончанием строки (используется параметр end).

## Самостоятельная работа №6
### В предложении 'Hello World' вставьте 'my' между двумя словами. Выведите полученное предложение в консоль в одну строку. Программа должна занимать не более двух строк редактора кода.

```python
line = "Hello World"
print(line[:5], 'my', line[6:], sep=' ')
```

### Результат

![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_2/images2/16.png)

### Выводы
  
Строка `print(line[:5], 'my', line[6:], sep=' ')`:

1. line[:5] – это срез строки, который берет первые пять символов, что дает "Hello".
2. 'my' – это просто строка, которая будет добавлена к выводу.
3. line[6:] – это срез строки, который берет все символы начиная с индекса 6 (седьмой символ), что дает "World".

## Самостоятельная работа №7
### Узнайте длину предложения 'Hello World', результат выведите в консоль. Программа занимать не более двух строк редактора кода.

```python
line = 'Hello world'
print(len(line))
```

### Результат

![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_2/images2/17.png)

### Выводы

Строка line содержит текст 'Hello world'. Функция len(line) возвращает длину строки, которая включает все символы, включая пробел

## Самостоятельная работа №8
### Переведите предложение 'HELLO WORLD' в нижний регистр. Программа должна занимать не более двух строк редактора кода.

```python
line = 'HELLO WORLD'
print(line.lower())
```

### Результат

![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_2/images2/18.png)

### Выводы

Строка line содержит текст 'HELLO WORLD'. Метод lower() используется для преобразования всех символов в строке в нижний регистр.
Когда вызываем line.lower(), строка 'HELLO WORLD' преобразуется в 'hello world'.

## Самостоятельная работа №9
### Самостоятельно придумайте задачу по проходимой теме и решите ее. Задача должна быть связанна со взаимодействием с числовыми значениями.
#### Напишите программу на Python, которая принимает два числа (a и b) от пользователя и выводит сумму, разность, произведение и частное этих чисел.

```python
a = int(input('Введите первое число: '))
b = int(input('Введите второе число: '))
print(f'Результат сложения: {a+b}')
print(f'Результат разности: {a-b}')
print(f'Результат произведения: {a*b}')
print(f'Результат деления: {a/b}')
```

### Результат

![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_2/images2/19.png)

### Выводы

1. `a = int(input('Введите первое число: '))` – запрашивает у пользователя ввод первого числа и преобразует его в целое число.

2. `b = int(input('Введите второе число: '))` – запрашивает у пользователя ввод второго числа и также преобразует его в целое число.

3. `print(f'Результат сложения: {a+b}')` – выводит результат сложения двух чисел.

4. `print(f'Результат разности: {a-b}')` – выводит результат вычитания второго числа из первого.

5. `print(f'Результат произведения: {a*b}')` – выводит результат умножения двух чисел.

6. `print(f'Результат деления: {a/b}')` – выводит результат деления первого числа на второе.

## Самостоятельная работа №10
### Самостоятельно придумайте задачу по проходимой теме и решите ее. Задача должна быть связанна со взаимодействием со строковыми значениями.
#### Заменить в строке "Hello World" все буквы "l" на '1' и сделать строку в верхнем регистре. Программа должна занимать не более двух строк редактора кода.

```python
line = 'Hello World'
print(line.replace('l', '1').upper())
```

### Результат

![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_2/images2/20.png)

### Выводы

1. `line.replace('l', '1')` – этот метод заменяет все символы 'l' в строке на символ '1'. В результате, строка 'Hello World' станет 'He11o Wor1d'.

2. Затем, метод upper() применяется к полученной строке. Этот метод преобразует все символы в строке в верхний регистр.

Таким образом, после применения upper() строка 'He11o Wor1d' преобразуется в 'HE11O WOR1D'.