# Тема 10. Декораторы и исключения
Отчет по Теме #10 выполнил(а):
- Ганиева Анастасия Ильсуровна
- ПИЭ-22-1

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | + |
| Задание 4 | + | + |
| Задание 5 | + | + |

знак "+" - задание выполнено; знак "-" - задание не выполнено;

Работу проверили:
- к.э.н., доцент Панов М.А.

## Лабораторная работа №1

Наверняка вы думаете, что декораторы – это какая-то бесполезная вещь, которая вам никогда не пригодится, но тут вдруг на паре по математике преподаватель просит всех посчитать число Фибоначчи для 100. Кто-то будет считать вручную (так точно не нужно), кто-то посчитает на калькуляторе, а кто-то подумает, что он самый крутой и напишет рекурсивную программу на Python и немного огорчится, потому что данная программа будет достаточно долго считаться, если ее просто так запускать. Но именно тут к вам на помощь приходят декораторы, например @lru_cache (он предназначен для решения задач динамическим программированием, если простыми словами, то этот декоратор запоминает промежуточные результаты и при рекурсивном вызове функции программа не будет считать одни и те же значения, а просто “возьмёт их из этого декоратора”). Вам нужно написать программу, которая будет считать числа Фибоначчи для 100 и запустить ее без этого декоратора и с ним, посмотреть на разницу во времени решения поставленной задачи.
P.S. при запуске без декоратора можете долго не ждать, для наглядности хватит 10 секунд ожидания.

```python
from functools import lru_cache

@lru_cache(None)
def fibonacci(n):
    if n==0:
        return 0
    elif n==1:
        return 1
    return fibonacci(n-1) + fibonacci(n-2)

if __name__ == '__main__':
    print(fibonacci(100))
```
### Результат.
![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_10/images10/1.png)

## Выводы

В коде реализована функция для вычисления чисел Фибоначчи с использованием декоратора lru_cache из модуля functools.
1. `from functools import lru_cache`  импортируем декоратор lru_cache, который позволяет кэшировать результаты вызовов функции. Это означает, что если функция вызывается с одним и тем же аргументом, результат будет возвращен из кэша, а не вычисляться заново. 
2. Определяем функцию fibonacci, которая принимает один аргумент n.
- Если n равно 0, функция возвращает 0 (это первое число Фибоначчи).
- Если n равно 1, функция возвращает 1.
- Для всех остальных значений n, функция вызывает саму себя рекурсивно для вычисления суммы двух предыдущих чисел Фибоначчи: `fibonacci(n-1)` и `fibonacci(n-2)`.
3. Результат вычисления 100-го числа Фибоначчи выводится на экран.

## Лабораторная работа №2

Илья пишет свой сайт и ему необходимо сделать минимальную проверку ввода данных пользователя при регистрации. Для этого он реализовал функцию, которая выводит данные пользователя на экран и решил, что будет проверять правильность введённых данных при помощи декоратора, но в этом ему потребовалась ваша помощь. Напишите декоратор для функции, который будет принимать все параметры вызываемой функции (имя, возраст) и проверять чтобы возраст был больше 0 и меньше 130. 
Причем заметьте, что неважно сколько пользователь введет данных на сайт к Илье, будут обрабатываться только первые 2 аргумента. 

```python
def check(input_func):
    def output_func(*args):
        name, age = args[0], args[1]

        if age < 0 or age > 130:
            age = 'Недопустимый возраст'
        input_func(name, age)

    return output_func

@check
def personal_info(name, age):
    print(f"Name: {name} Age: {age}")

personal_info('Анастасия', 20)
personal_info('Надежда', -50)
personal_info('Петр', 138, 15, 48, 2)
```
### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_10/images10/2.png)

## Выводы

В коде определена функция-декоратор check, которая проверяет допустимость возраста перед передачей его в функцию personal_info.
1. Вызов функции `personal_info` с аргументами 'Анастасия' и 20. Возраст 20 допустимый (между 0 и 130), поэтому будет выведено: `Name: Анастасия Age: 20`
2. Вызов функции `personal_info` с аргументами 'Надежда' и -50. Возраст -50 недопустимый (меньше 0), поэтому в переменной age будет присвоено значение `'Недопустимый возраст'`.
3. Вызов функции `personal_info` с аргументами 'Петр', 138, 15, 48, и 2. Декоратор check будет обрабатывать только первые два аргумента (name и age), а остальные будут проигнорированы Возраст 138 недопустимый (больше 130), поэтому в переменной age будет присвоено значение `'Недопустимый возраст'`.

## Лабораторная работа №3

Вам понравилась идея Ильи с сайтом, и вы решили дальше работать вместе с ним. Но вот в вашем проекте появилась проблема, кто-то пытается сломать вашу функцию с получением данных для сайта. Эта функция работает только с данными integer, а какой-то недохакер пытается все сломать и вместо нужного типа данных отправляет string. 
  
Воспользуйтесь исключениями, чтобы неподходящий тип данных не ломал ваш сайт. 
Также дополнительно можете обернуть весь код функции в try/except/finally для того, чтобы программа вас оповестила о том, что выявлена какая-то ошибка или программа успешно выполнена. 

```python
def data(*args):
    try:
        for i in range(len(*args)):
            try:
                result = (args[0][i] * 15) // 10
                print(result)
            except Exception as ex:
                print(ex)
    except Exception as ex:
        print(ex)
    finally:
        print('Вся информация обработана')

data([1, 15, 'Hello', 'i', 'try', 'to', 'crash', 'your', 'site', 38, 45])
```
### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_10/images10/3.png)

## Выводы

Функция data принимает произвольное количество аргументов, которые затем обрабатываются в цикле.
1. Внешний блок try: используется для обработки исключений.
2. Цикл for: проходит по каждому элементу списка, который передан как первый аргумент (args[0]).
3. Внутренний блок try: умножает каждый элемент списка на 15 и делить на 10. Если элемент не является числом (например, строка), то возникнет исключение.


## Лабораторная работа №4

 Продолжая работу над сайтом, вы решили написать собственное исключение, которое будет вызываться в случае, если в функцию проверки имени при регистрации передана строка длиннее десяти символов, а если имя имеет допустимую длину, то в консоль выводиться “Успешная регистрация” 

```python
class NegativeValueException(Exception):
    pass

def check_name(name):
    if len(name) > 10:
        raise NegativeValueException('Длина более 10 символов')
    else:
        print('Успешная регистрация')

name = '123456788910'
check_name(name)
```
### Результат.
![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_10/images10/4.png)

## Выводы

1. Создается новый класс исключения `NegativeValueException`, который является подклассом стандартного класса исключений Exception
2. Функция `check_name`: принимает параметр — name.
   - Внутри функции проверяется длина строки name. Если она больше 10 символов, вызывается исключение `NegativeValueException` с сообщением `'Длина более 10 символов'`.
   - Если длина строки меньше или равна 10 символам, выводится сообщение `'Успешная регистрация'`.
3.  При вызове функции `check_name` длина строки name равна 12, что больше 10. Это приведет к исключению `NegativeValueException`.

## Лабораторная работа №5

После запуска сайта вы поняли, что вам необходимо добавить логгер, для отслеживания его работы. Готовыми вариантами вы не захотели пользоваться, и поэтому решили создать очень простую пародию. Для этого создали две функции:   init  () (вызывается при создании класса декоратора в программе) и  call () (вызывается при вызове декоратора). Создайте необходимый вам декоратор. Выведите все логи в консоль.

```python
class SiteChecker:
    def __init__(self, func):
        print('> Класс SiteChecker метод __init__ успешный запуск')
        self.func = func

    def __call__(self):
        print('> Проверка перед запуском', self.func.__name__)
        self.func()
        print('> Проверка безопасного выключения')

@SiteChecker
def site():
    print('Усердная работа сайта')

print('>> Сайт запущен')
site()
print('>> Сайт выключен')
```
### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_10/images10/5.png)

## Выводы

1. В классе `SiteChecker` описаны два метода: `init` и `call`.
2. Метод `init` принимает функцию func в качестве аргумента и присваивает ее атрибуту `self.func`.
3. Метод `call`: выведет сообщение перед запуском функции, затем вызовет её и затем выведет сообщение о безопасном выключении.
4. Вызовы

## Самостоятельная работа №1

### Вовочка решил заняться спортивным программированием на python, но для этого он должен знать за какое время выполняется его программа. Он решил, что для этого ему идеально подойдет декоратор для функции, который будет выяснять за какое время выполняется та или иная функция. Помогите Вовочке в его начинаниях и напишите такой декоратор. Результатом вашей работы будет листинг кода и скриншот консоли, в котором будет выполненная функция Фибоначчи и время выполнения программы.

```python
import time

def timer_decorator(func):
    def wrapper(*args, **kwargs):
        start_time = time.time()
        result = func(*args, **kwargs)
        end_time = time.time()
        print(f'\nФункция была выполнена за {end_time - start_time} сек.')
        return result

    return wrapper

@timer_decorator
def fibonacci():
    fib1 = fib2 = 1
    for i in range(2, 400):
        fib1, fib2 = fib2, fib1 + fib2
        print(fib2, end=' ')

if __name__ == '__main__':
    fibonacci()
```

### Результат

![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_10/images10/11.png)

### Выводы

1. Импорт модуля `time`: модуль используется для измерения времени выполнения функции.
2. Декоратор `timer_decorator`: принимает функцию func в качестве аргумента. Внутри него определяется внешняя функция wrapper, которая измеряет время выполнения func и выводит его после завершения.
3. Функция `fibonacci`: вычисляет последовательность Фибоначчи, начиная с третьего элемента до 400-го. В результате `print(fib2, end=' ')` выводит элементы последовательности в строку без переноса.

## Самостоятельная работа №2

### Посмотрев на Вовочку, вы также загорелись идеей спортивного программирования, начав тренировки вы узнали, что для решения некоторых задач необходимо считывать данные из файлов. Но через некоторое время вы столкнулись с проблемой что файлы бывают пустыми, и вы не получаете вводные данные для решения задачи. После этого вы решили не просто считывать данные из файла, а всю конструкцию оборачивать в исключения, чтобы избежать такой проблемы. Создайте пустой файл и файл, в котором есть какая-то информация. Напишите код программы. Если файл пустой, то, нужно вызвать исключение (“бросить исключение”) и вывести в консоль “файл пустой”, а если он не пустой, то вывести информацию из файла.


```python
def read_file(filename):
    try:
        file = open(filename, 'r', encoding='utf-8-sig')
        content = file.read()
        if not content:
            raise Exception(f"{filename} пустой")
        print(content)
    except Exception as e:
        print(str(e))

if __name__ == '__main__':
    read_file('empty.txt')
    read_file('dontempty.txt')
```

### Результат

![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_10/images10/12.png)

### Выводы

1. Функция `read_file`: чтениу содержимого файла, заданного параметром `filename`.
2. После открытия файла содержимое считывается в переменную content.
3. Если файл пустой, создается и выбрасывается исключение с сообщением, что файл пуст. 
4. Если `dontempty.txt` существует и содержит текст, он будет выведен на экран.

  
## Самостоятельная работа №3
### Напишите функцию, которая будет складывать 2 и введенное пользователем число, но если пользователь введет строку или другой неподходящий тип данных, то в консоль выведется ошибка “Неподходящий тип данных. Ожидалось число.”. Реализовать функционал программы необходимо через try/except и подобрать правильный тип исключения. Создавать собственное исключение нельзя. Проведите несколько тестов, в которых исключение вызывается и нет. Результатом выполнения задачи будет листинг кода и получившийся вывод в консоль.

```python
def sum_num():
    try:
        num = float(input('Введите число:\n'))
        res = num + 2
        return res
    except ValueError:
        return 'Неподходящий тип данных. Ожидалось число.'

print(sum_num())
print(sum_num())
```

### Результат

![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_10/images10/13.png)


### Выводы

1. Функция sum_num(): запрашивает у пользователя ввод числа с помощью input(). Введенное значение пытается преобразовать в число с плавающей запятой (float). Если преобразование прошло успешно, к числу прибавляется 2, и результат возвращается. Если пользователь ввел некорректное значение (текст или символ), возникает исключение ValueError, и функция возвращает сообщение об ошибке.
2. Функция вызывается дважды, и результаты каждого вызова выводятся на экран с помощью print().

  
## Самостоятельная работа №4

### Создайте собственный декоратор, который будет использоваться для двух любых вами придуманных функций. Декораторы, которые использовались ранее в работе нельзя воссоздавать. Результатом выполнения задачи будет: класс декоратора, две как-то связанными с ним функциями, скриншот консоли с выполненной программой и подробные комментарии, которые будут описывать работу вашего кода.
  
```python
import time

# Декоратор для измерения времени выполнения функции
class Timer:
    def __call__(self, func):
        def wrapped_func(*args, **kwargs):
            start_time = time.time()  # запускаем таймер
            result = func(*args, **kwargs)  # выполняем оригинальную функцию
            end_time = time.time()  # останавливаем таймер
            print(f'Время выполнения функции {func.__name__}: {end_time - start_time:.4f} секунд')
            return result  # возвращаем результат функции

        return wrapped_func

# функция для вычисления квадрата
@Timer()
def calculate_square(num):
    time.sleep(0.5)
    return num ** 2

# функция для вычисления куба
@Timer()
def calculate_cube(num):
    time.sleep(0.5)
    return num ** 3

# тестирование функций
print(calculate_square(6))
print(calculate_cube(8))

```

### Результат

![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_10/images10/14.png)

### Выводы

1. Класс `Timer:` класс реализует декоратор для измерения времени выполнения функций.
2. Метод `__call__`: метод, который позволяет экземплярам класса вести себя как функции. Он принимает одну функцию func, которую нужно декорировать.
3. Функция `wrapped_func`: функция, которая принимает произвольное количество позиционных (*args) и именованных (**kwargs) аргументов.
- `start_time = time.time()`: запоминает текущее время перед вызовом оригинальной функции.
- `result = func(*args, **kwargs)`: вызывает оригинальную функцию и сохраняет результат
- `end_time = time.time()`: запоминает текущее время после выполнения функции.
- `print()`: выводит время выполнения функции в формате `"Время выполнения функции {имя функции}: {время} секунд"`.
- `return result`: возвращает результат 
4. Функция `calculate_square` принимает один аргумент num и возвращает его квадрат.
5. Функция `calculate_cube` аналогичным образом принимает num и возвращает его куб.

## Самостоятельная работа №5

### Создайте собственное исключение, которое будет использоваться в двух любых фрагментах кода. Исключения, которые использовались ранее в работе нельзя воссоздавать. Результатом выполнения задачи будет: класс исключения, код к котором в двух местах используется это исключение, скриншот консоли с выполненной программой и подробные комментарии, которые будут описывать работу вашего кода.
  
```python
class NegativeValueError(Exception):
    """исключение, возникающее при передаче отрицательного значения."""
    pass
def calculate_square(num):
    """вычисляет квадрат числа. Выбрасывает исключение, если число отрицательное."""
    if num < 0:
        raise NegativeValueError(f"Ошибка: нельзя вычислить квадрат для отрицательного числа {num}.")
    return num ** 2

def calculate_cube(num):
    """вычисляет куб числа. Выбрасывает исключение, если число отрицательное."""
    if num < 0:
        raise NegativeValueError(f"Ошибка: нельзя вычислить куб для отрицательного числа {num}.")
    return num ** 3
def main():
    numbers = [12, -7, 6]  # список чисел для вычисления
    for num in numbers:
        try:
            square = calculate_square(num)
            print(f"Квадрат числа {num}: {square}")
        except NegativeValueError as e:
            print(e)  # обработка исключения для квадрата

        try:
            cube = calculate_cube(num)
            print(f"Куб числа {num}: {cube}")
        except NegativeValueError as e:
            print(e)  # обработка исключения для куба

if __name__ == "__main__":
    main()
```

### Результат

![Меню](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_10/images10/15.png)

### Выводы

- Создадим исключение, назовем его NegativeValueError. Это исключение будет использоваться в двух частях кода: в функции, которая вычисляет квадрат числа; и в функции, которая вычисляет куб числа. Если переданное число отрицательное, будет выбрасываться наше исключение.
1. Создаем класс NegativeValueError, который используется для обработки случаев, когда переданное значение не подходит (у нас — отрицательное).
2. Функции calculate_square и calculate_cube: проверяет входное значение. Если значение меньше нуля, выбрасывается исключение с описанием ошибки. Если значение положительное, возвращается результат вычисления (квадрат или куб).
3. Функция main:
- Создается список чисел для обработки.
- Для каждого числа выполняется попытка вычисления квадрата и куба.

  
## Общие выводы по теме