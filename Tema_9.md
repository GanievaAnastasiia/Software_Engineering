# Тема 9. Концепции и принципы ООП
Отчет по Теме #9 выполнил(а):
- Ганиева Анастасия Ильсуровна
- ПИЭ-22-1

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | - |
| Задание 3 | + | - |
| Задание 4 | + | - |
| Задание 5 | + | - |


знак "+" - задание выполнено; знак "-" - задание не выполнено;

Работу проверили:
- к.э.н., доцент Панов М.А.

## Лабораторная работа №1

Допустим, что вы решили оригинально и немного странно познакомится с человеком. Для этого у вас должен быть написан свой класс на Python, который будет проверять угадал ваше имя человек или нет. Для этого создайте класс, указав в свойствах только имя. Дальше создайте функцию init (), а в ней сделайте проверку на то угадал человек ваше имя или нет. Также можете проверить что будет, если в этой функции указав атрибут, который не указан в вашем классе, например, попробуйте вызвать фамилию.

```python
class Ivan:
    __slots____ = ['name']
    def __init__(self, name):
        if name == 'Иван':
            self.name = f"Дa, я {name}"
        else:
            self.name = f"Я не {name}, а Иван"
person1 = Ivan('Алексей')
person2 = Ivan('Иван')
print(person1.name)
print(person2.name)
person2.surname = 'Петров'
```
### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_9/images9/1.png)

## Выводы

1. При создании экземпляра класса Ivan с именем происходит проверка:
   - Если переданное имя равно 'Иван', то атрибут name получает значение: `"Дa, я Иван"`.
   - Если нет, то атрибут name получает значение: `"Я не {name}, а Иван"`.
2. Создание объектов: Создаются два экземпляра класса Ivan:
   - `person1` с именем 'Алексей'
   - `person2` с именем 'Иван'
3. С помощью функции `print()` выводятся значения атрибута name для каждого объекта.

## Лабораторная работа №2

Вам дали важное задание, написать продавцу мороженого программу, которая будет писать добавили ли топпинг в мороженое и цену после возможного изменения. Для этого вам нужно написать класс, в котором будет определяться изменили ли состав мороженого или нет. В этом классе реализуйте метод, выводящий на печать «Мороженое с {ТОППИНГ}» в случае наличия добавки, а иначе отобразится следующая фраза: «Обычное мороженое». При этом программа должна воспринимать как топпинг только атрибуты типа string.

```python
class Icecream:
    def __init__(self, ingredient=None):
        if isinstance(ingredient, str):
            self.ingredient = ingredient
        else:
            self.ingredient = None
    def composition(self):
        if self.ingredient:
            print(f"Мороженое с {self.ingredient}")
        else:
            print('Обычное мороженное')
icecream = Icecream()
icecream.composition()
icecream = Icecream('шоколадом')
icecream.composition()
icecream = Icecream(5)
icecream.composition()
```
### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_9/images9/2.png)

## Выводы

1. В классе  `Icecream` определён метод `init`. Если аргумент `ingredient` является строкой, то он сохраняется в атрибуте `self.ingredient`. В противном случае в `self.ingredient` устанавливается в `None`.
2. Метод `composition` проверяет, есть ли в объекте действительный ингредиент. Если да, выводится сообщение с указанием ингредиента, если нет тогда выводится `"Обычное мороженное"`.
3. Создаётся новый объект `icecream` с ингредиентом `шоколадом`. 
5. Создание объекта с неправильным ингредиентом: объект `icecream` с ингредиентом 5. Метод инициализации устанавливает `self.ingredient` в `None`, потому что 5 не является строкой. Метод `composition` выводит `"Обычное мороженное"`.


## Лабораторная работа №3

Петя – начинающий программист и на занятиях ему сказали реализовать икапсу…что-то. А вы хороший друг Пети и ко всему прочему прекрасно знаете, что икапсу…что-то – это инкапсуляция, поэтому решаете помочь вашему другу с написанием класса с инкапсуляцией. Ваш класс будет не просто инкапсуляцией, а классом с сеттером, геттером и деструктором. После написания класса вам необходимо продемонстрировать что все написанные вами функции работают.

```python
class MyClass:
    def __init__(self, value):
        self._value = value

    def set_value(self, value):
        self._value = value

    def get_value(self):
        return self._value

    def del_value(self):
        del self._value

    value = property(get_value, set_value, del_value, "Свойство value")

obj = MyClass(42)
print(obj.get_value())
obj.value = 45
print(obj.get_value())
obj.value = 100
print(obj.get_value())
obj.del_value() 
```
### Результат.
![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_9/images9/3.png)

## Выводы

1. Определение класса `MyClass`:
   - Определены три метода:
     - `set_value`: устанавливает значение атрибута `_value`.
     - `get_value`: возвращает текущее значение атрибута `_value`.
     - `del_value`: удаляет атрибут `_value`.
2. Свойство value использует методы `get_value`, `set_value` и `del_value`. 
3. Создаётся объект `obj` класса `MyClass` с начальным значением 42.
4. Использование сеттера: используется свойство value для установки значения 45; `get_value` возращает 45 и выводит это значение. (С значением 100 происходит аналогичная процедура)
5. Метод `del_value` нужен для удаления `_value`. 

- Почему возникла ошибка? Из-за неправильного использования свойства: Методы set_value() и del_value() должны вызываться через свойство value, а не напрямую. Например, для установки значения используется obj.value = 45, а не set_value(45).

## Лабораторная работа №4

Вам прекрасно известно, что кошки и собаки являются млекопитающими, но компьютер этого не понимает, поэтому вам нужно написать три класса: Кошки, Собаки, Млекопитающие. И при помощи “наследования” объяснить компьютеру что кошки и собаки – это млекопитающие. Также добавьте какой-нибудь свой атрибут для кошек и собак, чтобы показать, что они чем-то отличаются друг от друга.

```python
class Mammal:
    className = 'Mammal'

class Dog(Mammal):
    species = 'canine'
    sound = 'wow'
    
class Cat(Mammal):
    species = 'feline'
    sound = 'meow'

dog = Dog()
print (f"Dog is {dog.className}, but they say {dog.sound}")
cat = Cat()
print (f"Cat is {cat.className}, but they say {cat.sound}")
```
### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_9/images9/4.png)

## Выводы

1. Класс `Mammal` содержит атрибут `className = 'Mammal'`. Все подклассы этого класса будут иметь доступ к этому атрибуту.
2. Класс `Dog` наследует от класса `Mammal`. Он имеет все свойства и методы родительского класса. В классе `Dog` определены два атрибута: `species` и `sound`. Класс `Cat` построен аналогично.
3. Создаётся объект `dog` класса `Dog`. При вызове `dog.className` будет возвращено `'Mammal'`, так как класс `Dog` наследует этот атрибут от класса `Mammal`. При вызове `dog.sound` возвращается `'wow'`, так как этот атрибут определён в классе `Dog`. Тоже самое пооисходит и с `Cat`.

- Классы `Dog` и `Cat` наследуют от `Mammal`. Объекты этих классов имеют доступ к атрибутам родительского класса и своим собственным. В выоде для dog и cat можно увидеть принадлежность к классам и издаваемые звуки.

## Лабораторная работа №5

На разных языках здороваются по-разному, но суть остается одинаковой, люди друг с другом здороваются. Давайте вместе с вами реализуем программу с полиморфизмом, которая будет описывать всю суть первого предложения задачи. Для этого мы можем выбрать два языка, например, русский и английский и написать для них отдельные классы, в которых будет в виде атрибута слово, которым здороваются на этих языках. А также напишем функцию, которая будет выводить информацию о том, как на этих языках здороваются.
Заметьте, что для решения поставленной задачи мы использовали декоратор @staticmethod, поскольку нам не нужны обязательные параметры-ссылки вроде self.

```python
class Russian:
    @staticmethod
    def greeting():
        print("Приветик!")

class English:
    @staticmethod
    def greeting():
        print("Hello!")

def greet(language):
    language.greeting()
ivan = Russian()
greet(ivan)
John = English()
greet(John)
```
### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_9/images9/5.png)

## Выводы

1. Класс `Russian` содержит метод `greeting()`, который выводит строку `"Приветик!"`. Аналогичная ситуация для класса `English`. 
2. Функция `greet` принимает один аргумент `language`, который должен быть экземпляром класса с методом `greeting`.
3. Создаётся экземпляр класса `Russian` и присваивается переменной `ivan`.
4. Вызов функции `greet` с аргументом ivan приводит к вызову метода `greeting()` класса `Russian`, который выводит `"Приветик!"`. Аналогичная ситуация для класса `English`

- В коде задействован полиморфизм. Оба класса реализуют метод `greeting()`, который вызывает функцию `greet`. Функция `greet` использует переданный аргумент для вызова соответствующего метода.

## Самостоятельная работа №1
Садовник и помидоры.  
Классовая структура:  
Есть Помидор со следующими характеристиками:  
• Индекс  
• Стадия созревания (стадии: отсутствует, цветение, зеленый, красный)  
Помидор может:  
• Расти (переходить на следующую стадию созревания)  
• Предоставлять информацию о своей зрелости      
Есть Куст с помидорами, который:  
• Содержит список томатов, которые на нем растут 
А также может:  
• Расти вместе с томатами  
• Предоставлять информацию о зрелости всех томатов  
• Предоставлять урожай  
И также есть Садовник, который имеет:  
• Имя  
• Растение, за которым он ухаживает 
Он может:  
• Ухаживать за растением  
• Собирать с него урожай<br>

Тесты:<br>1) Вызовите справку по садоводству<br>2) Создайте объекты классов TomatoBush и Gardener<br>3) Используя объект класса Gardener, поухаживайте за кустом с помидорами<br>4) Попробуйте собрать урожай, когда томаты еще не дозрели.<br>Продолжайте ухаживать за ними<br>5) Соберите урожай.

```python
class Tomato:
    states = ['отсутствует', 'цветение', 'зеленый', 'красный']

    # Установка начального состояния роста
    def __init__(self, index):
        self._index = index
        self._state = Tomato.states[0]

    # Перевод томата на стандию созревания
    def grow(self):
        self._state = Tomato.states[(Tomato.states.index(self._state) + 1) % len(Tomato.states)]

    def is_ripe(self):
        return self._state == 'красный'

class TomatoBush:
    # Список всех томатов
    def __init__(self, num_tomatoes):
        self.tomatoes = [Tomato(i) for i in range(num_tomatoes)]

    # Рост томатов на кусте
    def grow_all(self):
        for tomato in self.tomatoes:
            tomato.grow()

    # Проверка спелости
    def all_are_ripe(self):
        return all(tomato.is_ripe() for tomato in self.tomatoes)

    # Очищение куста
    def give_away_all(self):
        self.tomatoes = []

class Gardener:
    # Определение свойств имя и растение
    def __init__(self, name, plant):
        self.name = name
        self._plant = plant

    # Уход за садом
    def work(self):
        self._plant.grow_all()
        print(f'{self.name} Позаботился(ась) о растении.')

    # Проверка спелости и сбор урожая. Если все спелые, то сбор урожая
    def harvest(self):
        if self._plant.all_are_ripe():
            print(f'Томаты красные. {self.name} собрал(а) урожай.')
            self._plant.give_away_all()
        else:
            print('Томаты ещё не красные')

    # Статический метод, который выводит справку по садоводству
    @staticmethod
    def knowledge_base():
        print('Справка по садоводству: Ухаживайте за растениями, поливайте их, своевременно собирайте готовый урожай.')

# Вызов справки по садоводству
Gardener.knowledge_base()

# Создание объектов классов TomatoBush и Gardener
tomato_bush = TomatoBush(int(input('Сколько томатов на кусте?: ')))
gardener = Gardener(input('Ваше имя: '), tomato_bush)

while len(tomato_bush.tomatoes) != 0:
    action = int(input('Выберите действие: 1 - позаботиться о саде; 2 - Попробовать собрать урожай: '))
    if action == 1:
        gardener.work()  # Уход за кустом
    elif action == 2:
        gardener.harvest()  # Сбор урожая
        print()
    else:
        print('Ошибка, значение не предполагает действия')
```
### Результат.

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_9/images9/11.png)

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_9/images9/12.png)

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_9/images9/13.png)

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_9/images9/14.png)

![image](https://github.com/GanievaAnastasiia/Software_Engineering/blob/Тема_9/images9/15.png)

## Выводы

- Как пользоваться?
1. Введите количество томатов на кусте.
2. Укажите ваше имя.
3. Выберите, хотите ли вы позаботиться о саде или собрать урожай.
- Программа продолжит работу, пока все томаты не будут собраны.

1. Класс Tomato:
   - Имеет статическое свойство states содержит стадии зрелости.
   - Конструктор __init__ инициализирует индекс и устанавливает начальное состояние томата.
   - Метод grow переводит томат на следующую стадию созревания.
   - Метод is_ripe определяет, созрел ли томат.
2. Класс TomatoBush:
   - Конструктор __init__ создает список томатов на основе переданного количества.
   - Метод grow_all переводит все томаты на следующий этап созревания.
   - Метод all_are_ripe проверяет, все ли томаты созрели, возвращая логическое значение.
   - Метод give_away_all очищает список томатов.
3. Класс Gardener:
   - Конструктор инициализирует имя садовода и куст томатов.

- Выполняя это задания на практике я получила понимание объектно-ориентированного программирования (ООП).

## Общие выводы по теме

1. В Python класс - это шаблон, который определяет состояние и поведение объектов определенного типа. Классы являются основным строительным блоком объектно-ориентированного программирования в Python.
2. Методы класса - это функции, определенные внутри класса, которые выполняют операции с объектами класса или его атрибутами.
3. В объектно-ориентированном программировании (ООП) экземпляры классов являются конкретными объектами, созданными на основе определения класса. Каждый экземпляр представляет отдельный и независимый объект, имеющий свои собственные свойства и методы.
4. В Python динамическое изменение экземпляров классов позволяет добавлять, изменять или удалять атрибуты и методы на существующих экземплярах классов. 
5. В Python статические и классовые методы предоставляют специальные типы методов, которые могут быть определены внутри класса и вызываться без необходимости создавать экземпляр класса. Эти методы предоставляют функциональность, связанную с классом, а не с конкретными экземплярами.
6. Инкапсуляция в ООП - это механизм, который позволяет скрывать внутренние детали реализации класса и предоставлять только необходимый интерфейс для взаимодействия с объектами.
7. Наследование позволяет создавать новые классы на основе уже существующих классов. 
8. Полиморфизм позволяет объектам разных классов обладать одинаковым интерфейсом, что упрощает работу с объектами различных типов данных.

- Выполняя задания из лабораторной и самостоятельной работы я смогла принить теорию на практике. 